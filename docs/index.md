![](_static/logo.png)

# Introduction

Welcome to the documentation of DKRZ-CERA!

This package provides an interface to the CERA database of the DKRZ (Deutsches Klimarechenzentrum).


## Getting Started

### Installation

#### Install via pip

The easiest way to install the package is via pip directly from this repository:

```bash
$ pip install git+https://github.com/markusritschel/dkrz-cera.git
```

#### Clone repo and install locally

Alternatively, clone the repo and use the *Make* targets provided.
First, run

```bash
make conda-env
# or alternatively
make install-requirements
```

to install the required packages either via `conda` or `pip`, followed by

```bash
make src-available
```

to make the project's routines (located in `src`) available for import.

### Usage

The Makefile contains the central entry points for common tasks related to this project.

For scripts, the package can be imported and used as follows:

```python
import dkrz_cera
```

### Test code

You can run

```bash
make tests
```

to run the tests via `pytest`.


## Getting Started
<!-- 
If using Sphinx, you need to embed the table of contents (TOC) on this page. You can hide it with the `:hidden:` directive if you do not want it displayed on the page but only in the sidebar.

Except from the `api/index` page, which is automatically generated by the `autoapi` extension, all pages in this documentation are written in Markdown. You can find the source files for these pages in the `docs/` directory of this project.
In the TOC, these pages are listed without the `.md` file extension. 
-->

I recommend to use `uv` as a dependency manager (read here for the details).
Then, getting set up is as simple as 
```bash
uv sync
```
to install dependencies and make the project's source code (located in `src`) available for import.

👉 Activate your virtual environment:
```bash
source .venv/bin/activate
```
You can verify that you're using the environment's Python executable by checking if
```bash
which python
```
points to `.venv/bin/activate`.



```{toctree}
:hidden:
:caption: Main navigation

getting-started
commands
```



## Contact

For any questions or issues, please contact me via git@markusritschel.de or open an [issue](https://github.com/markusritschel/dkrz-cera/issues).


```{toctree}
:hidden:
:caption: Main navigation 

example
```


```{toctree}
:hidden:
:caption: Project information

api/index
bibliography
license
README <readme>
CHANGELOG <changelog>
```

